name: Publish to Ansible Galaxy

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry-run (validate inputs, skip role import)"
        required: false
        default: "false"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: somaz94/ansible-galaxy-publish-action@v1
        with:
          type: role
          api_key: ${{ secrets.GALAXY_API_KEY }}
          namespace: somaz94
          name: ansible_user_management
          dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || 'false' }}
